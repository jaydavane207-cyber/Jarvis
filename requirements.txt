## JARVIS Dependencies v2.0
## Install: pip install -r requirements.txt

# ── Core server ──────────────────────────────────────────────────────────────
fastapi
uvicorn
websockets
pydantic
pydantic-settings

# ── HTTP clients ─────────────────────────────────────────────────────────────
requests
httpx
aiofiles                   # Async file operations (audit log)

# ── AI / LLM ─────────────────────────────────────────────────────────────────
anthropic                  # Cloud LLM (Anthropic Claude) — optional, needs API key

# ── Semantic memory ──────────────────────────────────────────────────────────
chromadb                   # Vector database — persistent, embedded
sentence-transformers      # Embedding model (all-MiniLM-L6-v2, ~80MB download)

# ── Web research ─────────────────────────────────────────────────────────────
duckduckgo-search          # No API key needed
beautifulsoup4             # HTML scraping for richer results

# ── Testing ──────────────────────────────────────────────────────────────────
pytest
pytest-asyncio

# ── Productivity & Data ──────────────────────────────────────────────────────
pandas
matplotlib
fpdf2
python-docx

# ── Security & Privacy ───────────────────────────────────────────────────────
cryptography               # Column-level encryption for sensitive DB fields
pyotp                      # Multi-Factor Authentication
qrcode[pil]                # MFA Setup QR Codes

# ── Cloud Database ───────────────────────────────────────────────────────────
supabase
gotrue

# ── Telephony ────────────────────────────────────────────────────────────────
twilio

# ── Trading / Stocks (PRD §5.5) ──────────────────────────────────────────────
yfinance                   # Free NSE/BSE market data (.NS / .BO suffix)
python-dateutil            # Date parsing for tax-lot calculations

# ── Earnings Report Ingestion (PRD §5.5) ─────────────────────────────────────
pdfminer.six               # PDF text extraction for quarterly reports

# ── Scam Detection — Optional (PRD §5.4) ─────────────────────────────────────
# vt-py                    # VirusTotal API — install if VIRUSTOTAL_API_KEY is set
#                          # pip install vt-py
